<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Bootstrap --> 
    <link 
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" 
        rel="stylesheet" 
        integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" 
        crossorigin="anonymous"
    >
    <title>LearningExperiments</title>
</head>
<body>
    <%- include('_navbar') %>
    
    <!-- ms for margin start and me for margin end-->
    <div class="ms-2 me-2">
        <h1>Computer Science Articles</h1>
        <a href="/cs/post" class="btn btn-primary mb-3">New Article</a>
        
        <% articles.forEach(article => { %>
            <div class="card mt-2">
                <div class="card-body">
                    <h4><%= article.title %></h4>
                    <p><%= article.markdown %></p>
                    <a href="<%= article.thread %>/<%= article.id %>" class="btn btn-info">Show Full</a>
                    <form action="<%= article.thread %>/<%= article.id %>?_method=DELETE" method="POST" class="d-inline">
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                </div>
            </div>
        <% }) %>
    </div>
    

</body>
</html>