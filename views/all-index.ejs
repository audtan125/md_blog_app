
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Bootstrap --> 
    <link 
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" 
        rel="stylesheet" 
        integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" 
        crossorigin="anonymous"
    >
    <title>All Articles</title>
</head>
<body>
    <%- include('_navbar') %>
    
    <!-- ms for margin start and me for margin end-->
    <div class="ms-2 me-2">
        <h1>All Articles</h1>
        <p class="alert alert-light">To post a new article, please choose a subject</p>
        
        <% Articles.forEach(article => { %>
            <div class="card mt-2">
                <div class="card-body">
                    <h4><%= article.title %></h4>
                    <p><%= article.markdown %></p>
                    <a href="<%= article.thread %>/<%= article.id %>" class="btn btn-info">Show Full</a>
                    <form action="<%= article.thread %>/<%= article.id %>?_method=DELETE" method="POST" class="d-inline">
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                    <span class="badge badge-secondary bg-secondary float-end"><%= article.thread %> article</span>
                </div>
            </div>
        <% }) %>
    </div>
    

</body>
</html>